rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function isAdmin() {
      return request.auth != null
        && request.auth.uid == "xPFk3LWZhjMlPbGf77otc4kiuUT2";
    }

    // Current public collections. Public reads remain enabled during Phase 1
    // so existing website queries continue to work without interruption.
    match /posts/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /results/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /admitCards/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /importantInformation/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /subCategories/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /siteSettings/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    // Temporary read-only compatibility collections. These preserve older
    // published links while the active data is audited and normalized.
    match /jobs/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /admissions/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /admission/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /admit-cards/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /admitcards/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /result/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /schemes/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /scheme/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /governmentSchemes/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /government-schemes/{documentId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    // Everything else is private to the approved administrator.
    match /{document=**} {
      allow read, write: if isAdmin();
    }
  }
}
